# SPDX-License-Identifier: MIT OR Apache-2.0
[package]
name = "rust-basic"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
# version pin satisfies cargo-deny's `wildcards = "deny"` rule.
sieve-core = { path = "../../crates/sieve-core", version = "0.3.0" }
